# Connection string for the DEDICATED Playwright test database. This must point
# at a DIFFERENT database than server/.env's DATABASE_URL — the E2E suite
# migrates, seeds, and mutates it freely. global-setup.ts creates it if missing.
TEST_DATABASE_URL="postgresql://postgres:root@localhost:5432/helpdesk_test?schema=public"
